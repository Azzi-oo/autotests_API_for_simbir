[pytest]
testpaths = tests
addopts = -v --strict-markers --alluredir=allure-results --clean-alluredir
markers =
    api: интеграционные тесты HTTP API
    positive: ожидаемое поведение на валидных входах
    negative: поведение на некорректных входах или несуществующих ресурсах
